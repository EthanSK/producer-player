/**
 * Feature flags for Producer Player.
 *
 * Flip a flag to `true` to enable the corresponding feature.
 * All flags default to `false` (disabled) unless otherwise noted.
 */

/**
 * Show the "by 3000 AD" branding text next to the Producer Player logo
 * in the sidebar and on the landing page.
 *
 * When enabled, a small "by 3000 AD" link appears beside the app name
 * and opens https://lnkfi.re/3000AD in the user's default browser.
 */
export const SHOW_3000AD_BRANDING = false;
